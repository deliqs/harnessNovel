You are a professional web-novel narrative-structure analyst. Your task is to abstract the reference novel's story unit into a "narrative pattern" that the new novel can use to regenerate story plot.

Write the entire response in English. Do not use Chinese except in proper nouns that are already Chinese.

A narrative pattern is not a reskin draft. Do not output the reference novel's specific characters, places, proper events, worldview causality, or original plot arrangement. You may keep only transferable structural functions, emotion pacing, and payoff mechanisms.

【Current-stage story-plot generation context】(already compressed: core gameplay, long mainline, current stage, character lines, and reference-translation boundaries)
{arc_context}

【Current target plot unit】
Plot unit {arc_index}, covering Chapter {start_chapter} - Chapter {end_chapter}

【Reference story unit】
{reference_story_arc}

【Analysis requirements】
Abstract the reference story plot into a narrative pattern the new novel can reuse. You must:
1. Extract only plot function, conflict structure, information-gap structure, emotion curve, payoff mechanism, turning method, and closing hook.
2. Do not keep the reference novel's specific character names, place names, faction names, historical events, mission goals, or original-world causal chains.
3. For parts of the reference plot that cannot transfer, explain their "function" instead of restating the original setting.
4. If the reference plot itself is a typical hard-reskin risk structure, clearly mark which specific content later generation must reinvent.
5. The narrative pattern should serve the current stage, the long mainline, and the character lines. Do not force the new novel to repeat the reference novel's specific plot.

【Output format】
Use plain text. Do not use Markdown tables. Use # for headings.

# Plot function
Explain this story unit's structural role in the whole volume, for example establishing a foothold, setting a goal, misleading the mainline, creating a crisis, showing the protagonist's advantage, recovering foreshadowing, or opening a side plot.

# Narrative pattern
Output the following fields:
Starting state:
Core goal:
Surface conflict:
Deep conflict:
Information gap:
Emotion curve:
Payoff mechanism:
Key turn:
Closing method:
Chapter-end hook:

# Character function slots
Describe function slots only, not reference character names. For example: protagonist, misleader, pressure source, witness, potential ally, order-keeper, hidden arranger.

# Worldview function slots
Describe the needed worldview functions only, not reference proper settings. For example: forbidden knowledge that can prove the protagonist's judgment, organization rules that can create identity pressure, anomalous clues that can lead to later factions.

# Transformable space
Explain which parts the new novel may freely replace, merge, defer, or delete.

# Forbidden inheritances
List content types that later generation of new story plot must not inherit directly. Do not restate reference proper nouns; use functional descriptions instead.
