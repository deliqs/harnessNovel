You are a long-form web-novel stage / instance designer. The book-length long mainline is already confirmed (see below). You only need to supply the stage roadmap (stage_roadmap_md).

Write the entire response in English. Do not use Chinese except in proper nouns that are already Chinese.

[User creative direction]
{creative_direction}

[Rough outline] (highest priority; includes core gameplay, phase outline, and main characters)
{rough_outline}

[Worldview]
{worldview}

[Confirmed book-length long mainline] (stages must hook into it and must not contradict it)
{long_mainline}

[Reference novel structure with segment IDs] (learn only the rhythm of stage advance and long-running suspense)
{reference_outline}

[Target world knowledge base] (optional)
{world_knowledge}

The reference novel is for learning structure only. Do not copy names of people, places, events, or worldbuilding proper nouns.

Output JSON only. Do not output explanation, a Markdown code fence, or any text outside the JSON. Format:
{{
  "stage_roadmap_md": "# Stage 1: Stage name\n\nPlanned chapters: 12-15 chapters\n\n...",
  "referenced_arc_paths": ["segment IDs actually referenced or mapped this time"]
}}

stage_roadmap_md requirements:
1. Do not write a document-level title (such as "# Stage roadmap"); the first line of stage_roadmap_md must be "# Stage 1: Stage name". Each stage uses a level-1 heading (flush left, only one #; do not use ## / ### / bold / lists / tables / code fences). The format must be exactly: # Stage N: Stage name (N increases from 1).
2. Each stage must include "Planned chapters", so later steps can infer chapter count; the format is: Planned chapters: 12-15 chapters (write the numbers).
3. Do not output "reserve stages for later" or "stage iteration rules".
4. Each stage must include these items: Stage function, Gameplay value, Long mainline advance, Stage rules, Short lines inside the stage (several small goals / small crises / small reversals / small payoffs / small hooks that can move emotion), Main resources, Main enemies/pressure, Allies/witnesses, Character nodes, End state, Aftermath and hooks, Must not happen yet.

Structure example:
# Stage 1: Name
Planned chapters:
Stage function:
Gameplay value:
Long mainline advance:
Stage rules:
Short lines inside the stage:
Main resources:
Main enemies/pressure:
Allies/witnesses:
Character nodes:
End state:
Aftermath and hooks:
Must not happen yet:

# Stage 2: Name
Output in the same format.

referenced_arc_paths may only contain segment IDs that actually appear in the input and that this stage design actually referenced or mapped; if none were used, return an empty array.
