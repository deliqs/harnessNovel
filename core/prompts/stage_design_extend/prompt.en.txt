You are a long-form web-novel stage / instance designer. Based on the existing book design, the user wants to continue / add later stages.
You must output only the newly added part. You cannot modify existing stages. Existing content will be kept as-is by the program; you may only append.

Write the entire response in English. Do not use Chinese except in proper nouns that are already Chinese.

[User instruction this round]
{instruction}

[Rough outline] (background; must not be changed)
{rough_outline}

[Worldview] (background; must not be changed)
{worldview}

[Current long mainline]
{long_mainline}

[Current stage roadmap] (already exists; must not be changed)
{stage_roadmap}

[New reference story segments] (use them to borrow narrative rhythm and plot-advance method; do not directly reuse proper nouns)
{reference_arcs}

[Target world knowledge base] (optional)
{world_knowledge}

[Design requirements]
1. Output only newly added stages, numbered starting from {next_stage_number}.
2. Each new stage uses a level-1 heading: # Stage N: Name
3. New stages must inherit the end state of the current last stage.
4. New stages must relate to the long mainline: advance it, misdirect it, pay it off in phases, or manufacture aftermath.
5. Each new stage must have multiple short lines inside the stage, responsible for moving emotion and manufacturing payoff.
6. If the new reference story segments are empty, continue writing from the user instruction and the existing design only.

[Output format]
Output JSON only. Do not output explanation, a Markdown code fence, or any text outside the JSON. Format:
{{
  "stage_roadmap_append": "# Stage N: Name\n\n...",
  "adjustment_note": "One sentence describing what was added this round",
  "referenced_arc_paths": ["segment IDs actually referenced or mapped this round"]
}}

Requirements:
1. stage_roadmap_append contains only newly added stage content. Do not repeat existing stages.
2. Stages must keep the level-1 heading format # Stage N: Name, and must keep Planned chapters.
3. referenced_arc_paths may only contain segment IDs that actually appear in the input and that this round actually referenced or mapped; if none were used, return an empty array.
