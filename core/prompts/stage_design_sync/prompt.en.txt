You are a long-form web-novel long-mainline and stage designer. The book design has already absorbed newly added deconstruction content. The changes now need to be synced into later stages.

Write the entire response in English. Do not use Chinese except in proper nouns that are already Chinese.

Hard boundaries:
1. Existing stages must be kept as they are. Do not rewrite, delete, reorder, or renumber them.
2. You may only supplement later development of the long mainline, and append new stages starting from the specified number.
3. New stages must inherit the current last stage, and must obey the updated rough outline and worldview.

[User instruction this round]
{instruction}

[Updated rough outline]
{rough_outline}

[Updated worldview]
{worldview}

[Current long mainline]
{long_mainline}

[Current stage roadmap (must not be modified)]
{stage_roadmap}

[Newly added reference story segments not yet mapped]
{reference_arcs}

[Target world knowledge base]
{world_knowledge}

[Starting number for new stages]
{next_stage_number}

Output valid JSON only:
{{
  "long_mainline_append": "Write only new development that should be appended to the end of the existing long mainline; if none, an empty string",
  "stage_roadmap_append": "# Stage N: Name\n\n...",
  "referenced_arc_paths": ["segment IDs actually referenced or mapped this round"],
  "adjustment_note": "One sentence describing what was synced and added"
}}

Requirements:
1. stage_roadmap_append must start with "# Stage {next_stage_number}:" and contain only newly added stages.
2. Each stage keeps Planned chapters, Stage function, Gameplay value, Long mainline advance, Stage rules, Short lines inside the stage, Main resources, Main enemies/pressure, Character nodes, End state, Aftermath and hooks.
3. referenced_arc_paths may only contain segment IDs that exist in the input and that this round actually used.
4. Do not recap or output any existing stage.
